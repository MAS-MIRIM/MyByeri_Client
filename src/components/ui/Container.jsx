const Container = ({ children }) => (
  <div
    style={{
      maxWidth: "36rem",
      margin: "0 auto",
      padding: "2rem 1.5rem 5rem 1.5rem",
    }}
  >
    {children}
  </div>
);

export default Container;
